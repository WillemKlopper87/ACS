import { defineConfig, devices } from "@playwright/test";

// Browser E2E (audit P2.1/P2.4). The API is mocked per-test via
// page.route, so these validate the console's own behaviour (routing,
// auth flow, error surfaces, accessibility) hermetically — the
// backend's contract is covered by the Go integration suites.
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: process.env.CI ? "list" : [["list"], ["html", { open: "never" }]],
  use: {
    baseURL: "http://localhost:4173",
    trace: "retain-on-failure",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "npm run build && npm run preview -- --port 4173 --strictPort",
    url: "http://localhost:4173",
    reuseExistingServer: !process.env.CI,
    timeout: 180_000,
  },
});
